[project]
name = "memory-lab-stacks"
version = "0.1.0"
description = "CDK stacks for the memory lab"
requires-python = ">=3.11"
dependencies = [
  "aws-cdk-lib>=2.0.0",
  "boto3>=1.34.0",
  "constructs>=10.0.0,<11.0.0",
]

